<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script>
        function goToProfiles() {
            var newUrl = "https://project-2-career-gift-givers.onrender.com" + "/profile"; // Append "/profile" to the current URL
            window.location.href = newUrl; // Load the new URL within the current webpage
        }
    </script>

    <style>
        /* Style for card views */
        .card {
            width: 200px;
            height: 150px;
            border-radius: 10px;
            margin: 10px;
            cursor: pointer;
            overflow: hidden;
        }
        /* Style for label */
        .label {
            position: relative;
            top: 50%;
            transform: translateY(-50%);
            text-align: center;
            font-size: 18px;
            color: black;
            font-weight: bold;
            padding: 10px;
        }

        .banner {
            background: linear-gradient(to right, #ff0000, #ff7f00, #ffff00, #00ff00, #0000ff, #4b0082, #9400d3); /* Rainbow colors */
            color: white; /* Text color */
            text-align: center; /* Center align text */
            padding: 50px 0; /* Add some padding for better visibility */
        }

        .banner h1 {
            font-size: 3em; /* Large font size */
            margin: 0; /* Remove default margin */
            font-weight: bold; /* Bold font */
        }
    
        /* Style for skill div */
        .skill {
            padding: 20px;
            margin: 10px;
            color: white;
            text-align: center;
            border-radius: 5px;
            font-family: Arial, sans-serif;
        }

        /* Additional styles for desktop */
        @media screen and (min-width: 768px) {
            .skill {
                width: 300px;
                height: 200px;
            }
        }

        /* Additional styles for mobile */
        @media screen and (max-width: 767px) {
            .skill {
                width: calc(100% - 20px);
            }
        }
    </style>
</head>
<body>

<div class="banner">
    <h1>Career Gift Givers</h1>
</div>
<br/>
<br/>
<br/>

{{!-- Render each skill details using the skill-details partial --}}
{{#each skills}}
    <div class="skill" style="background-color: #28a745;" onclick="goToProfiles();">
        <h3>{{name}}</h3>
        <p>{{description}}</p>
        <!-- Add more details as needed -->
    </div>
{{/each}}

</body>
</html>
