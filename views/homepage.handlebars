<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script>
        function goToProfiles() {
            var newUrl = "https://project-2-career-gift-givers.onrender.com" + "/profile"; // Append "/profile" to the current URL
            window.location.href = newUrl; // Load the new URL within the current webpage
        }
    </script>

    <style>
        /* Style for card views */
        .card {
            width: 200px;
            height: 150px;
            border-radius: 10px;
            margin: 10px;
            cursor: pointer;
            overflow: hidden;
        }
        /* Style for label */
        .label {
            position: relative;
            top: 50%;
            transform: translateY(-50%);
            text-align: center;
            font-size: 18px;
            color: black;
            font-weight: bold;
            padding: 10px;
        }

        .banner {
            background: linear-gradient(to right, #ff0000, #ff7f00, #ffff00, #00ff00, #0000ff, #4b0082, #9400d3); /* Rainbow colors */
            color: white; /* Text color */
            text-align: center; /* Center align text */
            padding: 50px 0; /* Add some padding for better visibility */
        }

        .banner h1 {
            font-size: 3em; /* Large font size */
            margin: 0; /* Remove default margin */
            font-weight: bold; /* Bold font */
        }
    </style>
</head>
<body>

<div class="banner">
    <h1>Career Gift Givers</h1>
</div>
<br/>
<br/>
<br/>
<div class="container">
    <div class="row justify-content-center">
        <!-- Red -->
        <div class="card col-sm-3" style="background-color: #FF0000;" onclick="goToProfiles()">
            <div class="label">JavaScript</div>
        </div>
        <!-- Orange -->
        <div class="card col-sm-3" style="background-color: #FFA500;" onclick="goToProfiles()">
            <div class="label">HTML</div>
        </div>
        <!-- Yellow -->
        <div class="card col-sm-3" style="background-color: #FFFF00;" onclick="goToProfiles()">
            <div class="label">CSS</div>
        </div>
        <!-- Green -->
        <div class="card col-sm-3" style="background-color: #008000;" onclick="goToProfiles()">
            <div class="label">Backend</div>
        </div>
        <!-- Blue -->
        <div class="card col-sm-3" style="background-color: #0000FF;" onclick="goToProfiles()">
            <div class="label">Microservices</div>
        </div>
        <!-- Indigo -->
        <div class="card col-sm-3" style="background-color: #4B0082;" onclick="goToProfiles()">
            <div class="label">Operating Systems</div>
        </div>
        <!-- Violet -->
        <div class="card col-sm-3" style="background-color: #EE82EE;" onclick="goToProfiles()">
            <div class="label">Databases</div>
        </div>
        <!-- Other colors can be added similarly -->
    </div>
</div>

</body>
</html>
